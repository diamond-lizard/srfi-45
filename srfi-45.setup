;;;; srfi-45.setup  -*- Hen -*-

(use setup-helper-mod)

(verify-extension-name "srfi-45")

(setup-shared-extension-module (extension-name) (extension-version "3.1.1")
  #:compile-options '(
    -disable-interrupts
    -fixnum-arithmetic
    -optimize-level 3 -debug-level 1
    -no-procedure-checks))
